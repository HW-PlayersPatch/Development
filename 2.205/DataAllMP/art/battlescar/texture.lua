-- see Data:texture.lua.help for help

outputFormat =
{
	version = 1,

	-- The border pixel's alpha need to be modified at load time so
	-- leave the format in RGBA32.  

	textureFormat = TF_RGBA32,
	mipmap = 1,
}
